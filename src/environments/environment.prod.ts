// TODO(Phase 13): point these at the deployed api-gateway.
export const environment = {
  production: true,
  apiUrl: 'https://gateway.sprintmodus.invalid',
  wsUrl: 'wss://gateway.sprintmodus.invalid',
};
